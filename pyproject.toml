[project]
name = "neptune-apex-mcp"
version = "0.1.0"
description = "MCP server for Neptune Systems Apex aquarium controller"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "requests>=2.28.0",
]

